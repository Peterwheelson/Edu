1. создать пользователя с помощью. пользователя открыть домашние директории, солздать фалй и написать любой текст сохранить файл (проделать с помощью Vim) (переключить на него и проделать все эти опреации)

> [!task]
> 
> ![](Pasted%20image%2020260303120143.png)
> 
> создал пользователя через useradd И создал ему домашнюю директорию с помощью опции -m. После этого понял, что не указал bash как стандартную оболочку и через usermod -s изменил путь к стандартной оболочке.
> 
> ![](Pasted%20image%2020260303120226.png)
> 
> Через `vim newfile.txt` в домашней директории user1 добавил файл. ввел `i`, чтобы войти в Insert mode, написал "Hello world", затем нажал `Esc`, ввел `:wq` в терминале, чтобы записать изменения и выйти из редактора. 


2. из под своего пользорватьеля зайти под root от рута отредактировать файл с помощью sudo с правами пользователя владельца. после окончания редактирования проверить владельца файла с помощью команды `ls -l`

> [!task]
> 
> ![](Pasted%20image%2020260303121811.png)
> 
> зашел в root из-под своего нового пользователя
> 
> ![](Pasted%20image%2020260303121714.png)
> 
> изменил файл из-под рута
> 
> ![](Pasted%20image%2020260303122017.png)
> 
> проверил владельца файла - вроде не поменялся. Все так же user1. Пытался несколько раз из-под root, из-под оригинального пользователя peterwheelson с  `sudo`, из-под root с `sudo`

3. вернуться обратно под своего пользоваптеля попробовать с помощью cat открыть файл по его прямому пути (описать почему ошибка)

> [!TASK]
> ошибки нет, файл не поменял права. Возможно я неправильно понял задание. От оригинального пользователя peterwheelson нельзя открыть файл потому что у него нет прав root и он не является владельцем файла (если это имелось ввиду)
> 
> ![](Pasted%20image%2020260303123402.png)
> 

4. с помощью команды usermod добавить своего пользователя в группу владельцев файла (проверить еще раз возможность открытия) попробовать скопировать файл к себе в домашнюю директорию

> [!TASK]
> 
> ![](Pasted%20image%2020260303174515.png)
> 
>  добавил себя в нужную группу по имени нового пользователя. Однако сразу изменения не применились, я все еще не мог открыть файл - только после перезапуска сеанса получилось до него добраться
> 
> ![](Pasted%20image%2020260303174644.png)
> 
>  добрался до файла и скопировал через `cp` в домашнюю директорию первого пользователя


5. посмотреть как работает команда `stat`

> [!TASK]
> посмотрел как работает команда `stat`. Это функция для просмотра развернутой информации по файлу или папке или процессу, так как он тоже является своего рода файлом.
> 
> Имя, размер в байтах, кол-во занимаемых блоков на диске, размер блока, тип файла, inode - индексный дескриптор, который хранит метаданные о файле или каталоге. Ссылки (как ярлыки?) - указывают какое количество файлов обращаются к одним и тем же данным на компьютере (сервере), access - права доступа, владелец файла и группа, время последнего чтения, последнего изменения, изменения метаданных, создания
> 
>  ![](Pasted%20image%2020260303175629.png)
> 

6. выдать новому пользователю права sudo и из-под него подключиться в рута

> [!TASK]
> 
> ![](Pasted%20image%2020260303180954.png)
> 
> выдал права `sudo` user1 (подключил к группе sudo)
> 
>![](Pasted%20image%2020260303181101.png)
>
>и из-под него подключился к руту
